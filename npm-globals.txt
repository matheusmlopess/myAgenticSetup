# Global npm packages to install
# One package per line. Lines starting with # are ignored.
# Format: <npm-package-name>[:<command-to-check>]
# If no :<command> is given, the package name itself is used as the command.

@mermaid-js/mermaid-cli:mmdc
@openai/codex:codex
